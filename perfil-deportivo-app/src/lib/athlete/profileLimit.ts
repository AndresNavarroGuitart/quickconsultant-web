// Tope de perfiles (AthleteProfile) por cuenta: el propio + el de un
// dependiente, o 2 dependientes. Un solo lugar para no desincronizar la
// UI (onboarding, nav) del chequeo real en la API.
export const MAX_PROFILES_PER_USER = 2;
